import Image from 'next/image'
import Link from 'next/link'
import { notFound } from 'next/navigation'
import { versions } from '@/lib/content'
export function generateStaticParams(){return versions.map(v=>({slug:v.slug}))}
export async function generateMetadata({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const v=versions.find(x=>x.slug===slug);return {title:v?.name??'Версия',description:v?.summary}}
export default async function Page({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const v=versions.find(x=>x.slug===slug);if(!v)notFound();return <main><article className="detail-hero"><div className="breadcrumbs"><Link href="/versions">Версии</Link><span>/</span><span>{v.name}</span></div><div className="detail-heading"><div><span className="eyebrow">{v.edition} Edition · {v.year}</span><h1>{v.name}</h1></div><p>{v.summary}</p></div><div className="detail-image"><Image src={v.image} alt={`Обновление ${v.name}`} fill priority sizes="(max-width: 900px) 100vw, 1200px" /></div></article><div className="detail-body"><div className="facts-grid"><div className="fact"><span>Дата выхода</span><strong>{v.date}</strong></div><div className="fact"><span>Издание</span><strong>{v.edition} Edition</strong></div></div><h2>ГЛАВНЫЕ ИЗМЕНЕНИЯ</h2><p>{v.features.join(' · ')}. Обновление заметно расширило возможности исследования, строительства и автоматизации.</p><h2>НОВЫЕ БЛОКИ</h2><div className="tag-row">{v.blocks.map(x=><span key={x}>{x}</span>)}</div><h2>НОВЫЕ МОБЫ</h2><div className="tag-row">{v.mobs.map(x=><span key={x}>{x}</span>)}</div></div></main>}
